
<div class="row">
    <div class="col-md-12">
<h2>{{currentWalk.walk_name}}</h2>
<h4>{{currentWalk.description}}</h4>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <a href="{{currentWalk.pdf_url}}" target="_blank">Download Walk PDF</a>
    </div>

</div>

{{> activities}}

{{> pack-list}}

<iframe src="{{currentWalk.alerts}}" width='400px'>
</iframe>
